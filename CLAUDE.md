# 椎葉村議会 議事録自動生成システム

## プロジェクト概要

音声データ（MP3/M4A）と会議日程PDF を入力として、椎葉村議会の議事録 Word ファイル（.docx）を自動生成するバックエンドシステム。

## SUCCESS CRITERIA

- [ ] MP3/M4A 音声ファイルを文字起こしできる
- [ ] PDF から会議名・日付・議員名・日程を抽出できる
- [ ] `samples/001/expected_output/minutes.docx` と同等の Word ファイルを出力できる
- [ ] 発言者・発言内容を正しく分離できる
- [ ] 議事録のフォーマットルールをすべて守れている

## 議事録フォーマットルール

### 文書構成（上から順）

1. **告示部分**（招集告示・日付・村長名）
2. **応招議員リスト**（`〇開会日に応招した議員`）
3. **欠席議員リスト**（`〇応招しなかった議員`）
4. **タイトル行**（Titleスタイル）例：`令和７年第１回（臨時会）椎葉村議会会議録（第１日）`
5. **開催日時**
6. **議事日程リスト**（`議事日程（第１号）` → `日程第N　内容`）
7. **出席議員・欠席議員・欠員**
8. **事務局出席職員**
9. **説明のため出席した者**
10. **本文**（開会〜閉会）

### 発言行のフォーマット

```
〇役職名（氏名君）　発言内容
```

例：
```
〇議長（岡村　正司君）　日程第１、会議録署名議員の指名を行います。
〇総務課長（松岡　正社君）　おはようございます。…
```

### 特殊行のフォーマット

| 種類 | 書式 |
|---|---|
| 日程見出し | `日程第N　タイトル` |
| 休憩 | `午前HH時MM分休憩　` |
| 再開 | `午前HH時MM分再開` |
| 傍聴発言 | `（「異議なし」と呼ぶ者あり）` |
| 採決結果 | 地の文で記載 |

### Word スタイル

- **Title**: 会議録タイトル行のみ
- **Normal**: 全てのその他の行

### 氏名の表記

- 議員：`氏名君`（姓と名の間に全角スペース）
- 役職付き：`役職名（氏名君）`

## ディレクトリ構成

```
samples/
├── 001/
│   ├── input/
│   │   ├── audio.mp3        # 音声（MP3/M4A）
│   │   └── schedule.pdf     # 会議日程PDF
│   ├── expected_output/
│   │   └── minutes.docx     # 期待出力
│   └── README.md
src/
├── transcribe.py            # 音声→文字起こし
├── parse_pdf.py             # PDF→構造データ抽出
├── generate_minutes.py      # 構造データ→Word生成
└── main.py                  # エントリーポイント
```

## 技術スタック

- Python 3.11+
- 音声文字起こし: OpenAI Whisper（`openai-whisper` または `faster-whisper`）
- PDF解析: `pdfplumber` または `pypdf`
- Word生成: `python-docx`

## 開発ルール

1. `samples/` のファイルを変更しない（参照のみ）
2. 新機能追加前に必ず `samples/001` でテストを通す
3. 出力 Word は `samples/001/expected_output/minutes.docx` の構造に準拠する
4. 型ヒントを必ずつける
5. 関数単位でテストを書く（`tests/` フォルダ）
